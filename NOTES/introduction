25/10/2024

    HTML ==> hyper text markup language
        Is used for the structure/skeleton of a web page.HTML started by tim berners lee.
        HTML started by tim berner's lee.
        GML ----> SGML ----> HTML. current version of HTML is HTML 5 .
        HTML cinsists of tags. Tags are represented in two ways
        1.Open tag(<tag name>)
        2.close tag(</tag name>)
        <Human> </Human>
        whole HTML code is between HTML open tag and close tag.
        HTML contains two elements such as head and body.head also contains title. 

26/10/2024     

    we can generate basic templete using emmets such as !+enter,html:5+enter
    the gap on left of elements to make code more redable is known as indentation

    task 1:
    write a comment stating it is ur first HTML, create HTML template with proper intendation, give proper title and print in body, hello world