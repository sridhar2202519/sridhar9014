anchor tag
 anchor tag is used to link other web pages?resources.
 we have a property/attribute to give the link as 'href'.
 we use a tag for this.to open new tab we can use 'targer'
  attribute with '_blank' value
  EG: <a href="link">text</a>
  <a href="file path" download >text</a>
  you can also ude mailto and tel for different protocols

  img:images can be inserted into HTML by using img tag.
  <img src="path" alt+"">
  img is a self closing tag.They also called empty elements/unpaired tags
  src attribute contains path,when src failed to load th image,
  alt text will be printed

  FORMATING TAGS:
    Formating tags are usewd to format the text
    b-- used to make text bold
    strong -- used to make the text bold but search engines give
             more importance to it than b tag
     i -- used to make the text italic
     em -- used to make the text italic but search engines
           give more importance to it than i
      u -- is used to make text underline
      del -- is used make text striked out(according to search engine it is deleted)
      ins -- used to make the text inserted according to search engine but it
             appear5s as underlined
      sup -- is used to write superscript
      sub -- is used to write subscript
      br -- used to break the line
      hr -- is used to make horizontal line
      mark --- used to mark the text or highlight the text
      pre -- used to write pre-formated text
      code -- used to write codes
             